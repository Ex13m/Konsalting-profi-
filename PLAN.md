# План работ

`версия 1.0.0 · 16.09.2026`

Что делаем после одобрения и в каком порядке. Порядок не случайный:
каждый следующий пункт опирается на предыдущий, и пока не закрыт этап 0,
всё остальное работает вхолостую.

Отметки: `[ ]` не начато · `[~]` в работе · `[x]` готово.
Кто делает: **я** — в репозитории, **вы** — там, куда у меня нет доступа.

---

## Этап 0. Предусловия

Без этого нет смысла ни в SEO, ни в рекламе: работа идёт в старую
страницу или в чёрную дыру.

- [ ] **Одобрение.** Всё ниже стоит на паузе до вашего слова. — вы
- [ ] **Почта конторы.** Подтвердить домен в Resend, задать
      `RESEND_API_KEY`, `RESEND_FROM`, `OBJEDNANI_MAIL`. — вы
      *Зачем именно сейчас:* без ключа запись сохраняется, но в конторе
      о ней не узнают. Пока сайт на netlify-адресе это безобидно, после
      переезда — потеря клиента.
      **Готово, когда:** тестовая запись дошла письмом и в контору,
      и клиенту, с приглашением `.ics`.
- [ ] **Домен `konsalting.cz` → Netlify**, проект `konsalting`. — вы
      **Готово, когда:** домен отдаёт новый сайт, в подвале видна версия,
      сертификат выпущен, `konsalting.cz/api/objednat` отвечает.
- [ ] Удалить пустой проект `konsalting-profi` в Netlify, чтобы не
      привязать домен не туда. — вы
- [ ] Включить уведомление об исчерпании кредитов Netlify. — вы

---

## Этап 1. Чтобы находили

То, ради чего всё остальное. Разобрано по выдаче, которую сейчас
показывает Google.

- [ ] **Профиль в Google (Firemní profil).** Для локальной услуги карта
      стоит выше органики: человек ищет «účetní Praha 9», видит три
      карточки с рейтингом и звонит. — вы
      **Готово, когда:** профиль подтверждён, заполнены часы, услуги,
      адрес, телефон, ссылка на сайт, загружены фотографии и знак.
- [ ] **Отзывы.** Десять–пятнадцать настоящих отзывов от клиентов
      перевесят любую работу на сайте. — вы
      *Как:* короткая просьба в письме после закрытия квартала, ссылка
      прямо на форму отзыва.
- [ ] **Проверить и дозаполнить карточку на Firmy.cz.** Каталог Seznamu
      кормит его же поиск; карточка у вас уже есть. — вы
- [ ] **Разбить сайт на страницы под запросы.** Сейчас всё на одной,
      значит один набор ключевых слов. Бухгалтерию ищут раздельно. — я
      Отдельные адреса под: `vedení účetnictví`, `mzdová agenda`,
      `daňová evidence OSVČ`, `rekonstrukce zanedbaného účetnictví`.
      `cenik.html` уже такой — и «ceník účetnictví Praha» запрос
      с намерением купить, а цены публикуют единицы.
      **Готово, когда:** у каждой страницы свой заголовок, описание,
      текст и внутренние ссылки; в `sitemap.xml` перечислены все.
- [ ] **Налоговый календарь отдельной страницей.** «Daňový kalendář 2027»
      ищут тысячами каждый декабрь, и ищут ровно те, кому нужен
      бухгалтер. Сейчас это секция внутри главной. — я
      **Готово, когда:** `/danovy-kalendar` открывается сам, выгружается
      в `.ics`, ведёт на запись.
- [ ] **Языковые адреса и `hreflang`.** Переключение языка сейчас
      клиентское, адрес один — Google видит **только чешскую версию**.
      Русско- и украиноязычные предприниматели в Праге найти нас
      в своём языке не могут. — я
      **Готово, когда:** `/ru/`, `/uk/`, `/en/`, `/de/`, `/pl/` отдаются
      отдельно, связаны `hreflang`, каждая в индексе.
- [ ] **`sitemap.xml` и `robots.txt`.** — я
- [ ] Расширить структурированные данные: часы, диапазон цен, отзывы
      поверх нынешнего `AccountingService`. — я
- [ ] Подключить Google Search Console, посмотреть, по каким запросам
      показывают. Без этого дальше работаем вслепую. — вы

Реклама (Google Ads, Sklik) — **не раньше**, чем органика заработает
и появится куда вести. Иначе платим за показы страницы, которая
не убеждает.

---

## Этап 2. Чтобы записывались

- [ ] **Страница заявок.** Записи лежат в хранилище, посмотреть их
      негде. Пока идёт тест — это слепая зона. — я
      **Готово, когда:** защищённый адрес показывает список: дата, имя,
      телефон, почта, что нужно решить.
- [ ] **Календарь конторы.** Занятость берётся из записей на сайте,
      а не из реального календаря. Место готово: `window.KP_OBSAZENO`. — я + вы
      От вас: сервисный аккаунт Google и расшаренный на него календарь.
      **Готово, когда:** занятое в календаре закрыто на сайте, а новая
      запись появляется событием.
- [ ] **Формы обратного звонка** (`#leadform`, `#exitform`) никуда не
      отправляются, в отличие от формы записи. Привязать к тому же
      `/api/objednat`. — я

---

## Этап 3. Голос и лицо

- [ ] **Голосовая Клара.** Нужен Agent ID из ElevenLabs и ответ,
      публичный агент или с авторизацией. Всё остальное готово. — вы
- [ ] Включить `objednej_schuzku` в кабинете ElevenLabs — endpoint уже
      работает. — вы
- [ ] Липсинк по фонемам вместо нынешнего «рот по громкости» — задание
      и материалы отданы, ждём результата. — вы

---

## Этап 4. Порядок в хозяйстве

- [ ] **Юридическая вычитка** `ochrana-osobnich-udaju.html`. Текст писал
      я, юрист не смотрел. Отдельно: когда подключатся Resend и модели,
      их нужно назвать в списке обработчиков (ст. 28 GDPR). — вы
- [ ] **Публикуется весь репозиторий.** `publish = "."` отдаёт наружу
      `tools/`, `brand/`, `package.json` и исходники прототипа.
      Секретов там нет, но внутренняя кухня видна. — я
      **Готово, когда:** наружу уходит только сайт.
- [ ] **Приёмные дни заданы в двух местах** — в `index.html` и в
      `objednat.mjs`. Разойдутся — сайт предложит время, которое сервер
      отвергнет. Свести в один источник. — я
- [ ] Отправить теги `v0.1.0`…`v1.0.0` — они лежат локально, прокси
      среды их не пропускает: `git push --tags` с вашей машины. — вы
- [ ] Видео в шапке: положить `assets/hero.mp4`, и оно появится само.
      Необязательное. — вы

---

## Порядок одной строкой

Одобрение → почта → домен → профиль в Google и отзывы → страницы под
запросы → языковые адреса → календарь-магнит → заявки видно → календарь
конторы → реклама.
